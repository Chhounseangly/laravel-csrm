# Custom Stubs

Publish stubs to customize generated files:

```bash
php artisan vendor:publish --tag=csrm-stubs
```

Published stubs are placed in:

`stubs/csrm/`

When present, published stubs override package stubs.